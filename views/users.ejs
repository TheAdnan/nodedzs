<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
    <h1>Users:</h1>
    <% data.forEach(function(user) { %>
    <p><%= user.full_name %></p>
            <% }); %>
  </body>
</html>
